:ruby
  field_attrs = get_template_field_attributes local_assigns

.field.no_label_wrap_padding.type_date{field_attrs}
  .label_wrap
    .label= t(name, :scope => controller_scope_name) + ':'
  .value
    = resource.send(name).try(:strftime, I18n.t("format.datepicker.date", :default => '%Y-%m-%d'))
    -# FIXME
    -# = I18n.l(value, :format => 'date_picker')
  .clear
