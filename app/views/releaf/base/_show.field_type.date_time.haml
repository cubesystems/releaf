.field.no_label_wrap_padding.type_date_time{:'data-name' => name}
  .label_wrap
    .label= I18n.t(name.humanize, :scope => "models.#{item.class_name.underscore}") + ':'
  .value
    - value = item.send(name)
    -# FIXME
    = value
    -# = I18n.l(value, :format => 'datetime_picker')
