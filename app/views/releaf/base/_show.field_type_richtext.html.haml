:ruby
  field_attrs = get_template_field_attributes local_assigns

.field.no_label_wrap_padding.type_richtext{field_attrs}
  .label_wrap
    .label= t(name, :scope => controller_scope_name) + ':'
  .value
    .raw_html= raw resource.send(name)
  .clear
