.footer.center
  .padding
    .stats
      %span.total= resources.count \
      = t("resources_found", :scope => 'admin.global')

    %span.actions
      - if @features[:create]
        %a.button.create_new_item{:href => url_for(:controller => controller.controller_name, :action => "new")}
          = image_tag("releaf/icons/add.png", :alt => "add") \
          %span= t('create_new_item', :scope => 'admin.global')

      %span.button_group.list_action_switch
        - if @features[:show]
          %button{:type => "button", :title => t("switch_to_show", :scope => 'admin.global'), "data-action" => "show", :class => list_action == "show" ? "active" : ""}>
            = image_tag("releaf/icons/eye.png", :alt => t("switch_to_show", :scope => 'admin.global'))

        - if @features[:edit]
          %button{:type => "button", :title => t("switch_to_edit", :scope => 'admin.global'), "data-action" => "edit", :class => list_action == "edit" ? "active" : ""}>
            = image_tag("releaf/icons/pencil.png", :alt => t("switch_to_edit", :scope => 'admin.global'))

        - if @features[:destroy]
          %button{:type => "button", :title => t("switch_to_delete", :scope => 'admin.global'), "data-action" => "confirm_destroy", :class => list_action == "confirm_destroy" ? "active" : ""}
            = image_tag("releaf/icons/bin_empty.png", :alt => t("switch_to_delete", :scope => 'admin.global'))

    - unless @continuous_scroll
      - if resources.respond_to?(:page)
        = will_paginate( resources, :class=> "button_group pagination", :link_separator => "", :renderer => "Releaf::PaginationRenderer::LinkRenderer", :previous_label => "<", :next_label => ">", :outer_window => 0, :inner_window => 2 )
