= render :partial => "breadcrumbs"
= render :partial => "flash_notices"
%h2.header
  = resource.new_record? ? t('create_new_resource', :scope => 'admin.global') : resource_to_text(resource)
  .toolbox-wrap
    = toolbox(resource)
