%footer
  .tools
    .primary
      %button.button.primary{type:  "submit", data: { type: 'ok' } }
        %i.fa.fa-check
        %span= t('Save', scope: "admin.global")
    - if @features[:index]
      .secondary
        %a.button.secondary{ href: index_url }
          %i.fa.fa-caret-left
          = t('Back to list', scope: 'admin.global')
